import React from 'react'
import Button from '../button';

const ButtonStartGame = props => {
    return (
        <Button text="Play again" modifier="--large"/>
    );
};

export default ButtonStartGame;