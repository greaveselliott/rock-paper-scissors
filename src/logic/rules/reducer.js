import { ADD_ITEM } from './constants';

export const initialState = "rock-paper-scissors";

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
